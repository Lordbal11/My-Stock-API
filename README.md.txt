# Real-Time Stock API
This API fetches stock data from Alpha Vantage and includes Swagger documentation.

## How to use
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python app.py`
3. View Swagger docs at `/apidocs/`